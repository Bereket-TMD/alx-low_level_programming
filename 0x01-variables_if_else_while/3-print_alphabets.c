#include <stdio.h>

/**
 * main - function prints alphabet bot in upper and lower case.
 *
 * Return: 0
*/
int main(void)
	{
		char b;
	for (b = 'a'; b <= 'z'; b++)
	putchar(b);
	for (b = 'A'; b <= 'Z'; b++)
	putchar(b);
	return (0);
	}
