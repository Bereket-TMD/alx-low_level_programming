#include <stdio.h>

/**
	 * main - the function putchar from a to z.
	 * Return: 0 (success)
 */

int main(void)

	{
	char b;
		for (b = 'a'; b <= 'z'; b++)
		putchar(b);
		putchar('\n');

	return (0);

	}
